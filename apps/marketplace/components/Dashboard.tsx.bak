'use client';

import { useState, useEffect } from 'react';
import { motion } from 'framer-motion';
import dynamic from 'next/dynamic';
import ServiceCard from './ui/ServiceCard';

const UsageChart = dynamic(() => import('./analytics/UsageChart'), { ssr: false });

// import { X402Client } from 'x402-sdk'; // Assuming this is linked or available
// Since I can't easily link the local SDK in this environment without proper `npm link` or workspace setup detailed checks, 
// I will mock the data display for the UI demo purposes, but keep the structure ready for integration.

// Mock Data for UI Visualization
const MOCK_SERVICES = [
    { id: '1', name: 'GPT-4 Inference', description: 'High availability LLM inference endpoint with strict SLA.', price: '0.05', provider: '0x123...abc', reputation: 98 },
    { id: '2', name: 'Stable Diffusion XL', description: 'Fast image generation service optimized for latency.', price: '0.02', provider: '0x456...def', reputation: 92 },
    { id: '3', name: 'Llama 3 70B', description: 'Open source LLM hosting with verified compute credentials.', price: '0.01', provider: '0x789...ghi', reputation: 85 },
    { id: '4', name: 'Whisper Audio', description: 'Asynchronous audio transcription and translation.', price: '0.03', provider: '0xabc...123', reputation: 95 },
    { id: '5', name: 'Claude 3 Opus', description: 'Premium reasoning model endpoint.', price: '0.10', provider: '0xdef...456', reputation: 99 },
    { id: '6', name: 'Mistral Large', description: 'European sovereign model hosting.', price: '0.04', provider: '0xghi...789', reputation: 88 },
];

export default function Dashboard() {
    const [searchTerm, setSearchTerm] = useState('');

    const filteredServices = MOCK_SERVICES.filter(service =>
        service.name.toLowerCase().includes(searchTerm.toLowerCase()) ||
        service.description.toLowerCase().includes(searchTerm.toLowerCase())
    );

    return (
        <div className="max-w-7xl mx-auto">
            <header className="flex flex-col md:flex-row justify-between items-center mb-12">
                <div className="mb-6 md:mb-0">
                    <motion.h1
                        initial={{ opacity: 0, y: -20 }}
                        animate={{ opacity: 1, y: 0 }}
                        className="text-5xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-blue-400 via-purple-400 to-pink-400 mb-2"
                    >
                        x402 Market
                    </motion.h1>
                    <motion.p
                        initial={{ opacity: 0 }}
                        animate={{ opacity: 1 }}
                        transition={{ delay: 0.2 }}
                        className="text-gray-400"
                    >
                        Decentralized AI Service Protocol
                    </motion.p>
                </div>

                <div className="flex gap-4">
                    <motion.input
                        initial={{ width: 0, opacity: 0 }}
                        animate={{ width: '300px', opacity: 1 }}
                        transition={{ delay: 0.4 }}
                        type="text"
                        placeholder="Search services..."
                        value={searchTerm}
                        onChange={(e) => setSearchTerm(e.target.value)}
                        className="px-4 py-2 rounded-xl bg-white/5 border border-white/10 text-white focus:outline-none focus:border-blue-500/50 backdrop-blur-sm transition-all"
                    />
                    <motion.button
                        whileHover={{ scale: 1.05 }}
                        whileTap={{ scale: 0.95 }}
                        className="px-6 py-2 rounded-xl bg-gradient-to-r from-blue-600 to-purple-600 font-bold text-white shadow-lg hover:shadow-blue-500/20"
                    >
                        Connect Wallet
                    </motion.button>
                </div>
            </header>

            <div className="mb-12">
                <UsageChart />
            </div>

            <motion.div
                layout
                className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6"
            >
                {filteredServices.map((service, index) => (
                    <motion.div
                        key={service.id}
                        initial={{ opacity: 0, y: 20 }}
                        animate={{ opacity: 1, y: 0 }}
                        transition={{ delay: index * 0.1 }}
                    >
                        <ServiceCard
                            name={service.name}
                            description={service.description}
                            price={service.price}
                            provider={service.provider}
                            reputation={service.reputation}
                            onClick={() => console.log('Clicked', service.id)}
                        />
                    </motion.div>
                ))}
            </motion.div>
        </div>
    );
}
