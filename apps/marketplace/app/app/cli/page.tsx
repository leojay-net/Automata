'use client';

// CLI Reference page - redirects to main docs for now
// Could be expanded with interactive terminal demo

import DocsPage from '../docs/page';

export default function CLIPage() {
    return <DocsPage />;
}
